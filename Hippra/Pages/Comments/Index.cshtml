@page
@model Hippra.Pages.Comments.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CaseComment[0].PosterId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaseComment[0].PosterName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaseComment[0].posterSpeciality)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaseComment[0].Comment)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaseComment[0].PostedDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaseComment[0].Case)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CaseComment) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PosterId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PosterName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.posterSpeciality)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Comment)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PostedDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Case.ID)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
